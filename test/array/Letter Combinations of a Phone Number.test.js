import comb from '../../learning/array/Letter Combinations of a Phone Number'

test('Letter Combinations of a Phone Number', () => {
    expect(comb('23')).toEqual(['ad', 'ae', 'af', 'bd', 'be', 'bf', 'cd', 'ce', 'cf'])
})
